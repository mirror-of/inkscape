# Convenience stub makefile to call the real Makefile.

@SET_MAKE@

# Explicit so that it's the default rule.
all:
	cd .. && $(MAKE) potrace/all

clean %.a %.o:
	cd .. && $(MAKE) potrace/$@

.PHONY: all clean

OBJEXT = @OBJEXT@

.SUFFIXES:
.SUFFIXES: .a .$(OBJEXT)
